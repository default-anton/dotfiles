- Run `./install --no-brew` after modifying `pi/agent/commands/*.md`
- To verify changes to pi's skills, prompts, extensions, use `pi` in json mode:
```bash
command pi --thinking low --mode json -p "[YOUR PROMPT HERE]"
```
